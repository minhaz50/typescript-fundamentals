# Typescript Fundamentals

## How to install typescript

## To install typescript globally.

### npm install -g typescript

### Now command [ tsc --init ]in terminal. It will open tsconfig file. Now comment out of rootDir and outDir. You can keep folder name anything you want insted of src or dist. But it's recommended to keep the default folder name.
